# v1.4.1 — 点停止不再锁死输入框

修复 v1.4.0 的一处回归。

## 修复

- **点停止不再把会话报废。** 在 1.4.0 里，发出一条消息后，如果第一步还没跑完就按停止——或者停掉一条正在流式输出的纯文本回复——再打开侧栏，这个会话会被标成失败，输入框也锁死，既不能打字也不能再开一轮。原因是停止时没清掉内部的「任务进行中」标记，恢复扫描把它当成第一步崩溃。现在会清掉这个标记。已经被这次回归标成失败的会话不会自动恢复，新开一个即可。
