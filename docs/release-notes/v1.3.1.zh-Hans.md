# v1.3.1 — 「独立窗口」设置可以关掉了

修复 v1.3.0 新增的独立窗口模式。

## 修复

- **关闭「用独立窗口打开 Pie」现在真的生效了。** 一旦从右键菜单用独立窗口打开过 Pie，再去设置里取消勾选就不起作用——之后每次点击工具栏图标仍然弹出新窗口。原因是强制窗口模式的同时还记下了「这个浏览器无法显示侧边栏」的判定，而这个判定的存活期比设置项更长，导致 Pie 再也不会尝试侧边栏。现在切回自动会清掉该判定，下次点击重新检测。反方向同样修复：在侧边栏正常的浏览器上，从设置页**打开**这个开关原本也不生效，因为工具栏点击仍由浏览器自己处理。
