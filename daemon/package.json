{
  "name": "pie-daemon",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "compile": "bun build ./src/cli.ts --compile --outfile dist/pie"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  },
  "dependencies": {
    "@anthropic-ai/sandbox-runtime": "0.0.67",
    "yaml": "^2.9.0"
  }
}
